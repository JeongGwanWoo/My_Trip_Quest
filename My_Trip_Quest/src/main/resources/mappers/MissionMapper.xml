<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.tripquest.mapper.MissionMapper">

    <select id="findBySpotNoAndMissionType" resultType="com.ssafy.tripquest.domain.Mission">
        SELECT
            mission_id AS missionId,
            spot_no AS spotNo,
            mission_type AS missionType,
            reward_point AS rewardPoint,
            title
        FROM
            missions
        WHERE
            spot_no = #{spotNo} AND mission_type = #{missionType}
    </select>

    <select id="findById" parameterType="long" resultType="com.ssafy.tripquest.domain.Mission">
        SELECT
            mission_id AS missionId,
            spot_no AS spotNo,
            mission_type AS missionType,
            reward_point AS rewardPoint,
            title,
            description
        FROM
            missions
        WHERE
            mission_id = #{missionId}
    </select>

    <select id="findBySpotNo" resultType="com.ssafy.tripquest.domain.Mission">
        SELECT
            mission_id AS missionId,
            spot_no AS spotNo,
            mission_order AS missionOrder,
            unlock_condition AS unlockCondition,
            mission_type AS missionType,
            require_gps_verify AS requireGpsVerify,
            title,
            description,
            reward_point AS rewardPoint
        FROM
            missions
        WHERE
            spot_no = #{spotNo}
        ORDER BY
            mission_order
    </select>

</mapper>
